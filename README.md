# chama-manager

Chama (savings group) management system.

## Setup

```bash
cd chama-system
npm install
```

Copy `.env.example` to `.env` and configure your database credentials before running the server.
